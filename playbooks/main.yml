---

- hosts: all
  roles:
  - {role: base, tags: ['base']}

- hosts: all
  vars_files:
  - ../vars/user.yml
  - ../vaulted_vars/password.yml
  roles:
  - {role: user, tags: ['user']}

- hosts: all
  vars_files:
  - ../vars/user.yml
  roles:
  - {role: i3, tags: ['i3']}
  - {role: polybar, tags: ['polybar']}
  - {role: gtk, tags: ['gtk']}
